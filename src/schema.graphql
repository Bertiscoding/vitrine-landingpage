type Query {
 getAllUserInput: [UserInput]
 getUserInput(id: ID!): UserInput
}

type UserInput {
  id: ID!
  email: String!
  message: String
}

type Mutation {
 createUserInput(email: String!, message: String): UserInput
}
